# Optional origin-only API base. Leave absent for same-origin production calls.
NEXT_PUBLIC_API_BASE_URL=
# Development-only backend origin used by the Next.js rewrite; do not expose to browsers.
BACKEND_DEV_ORIGIN=http://127.0.0.1:4000
